# EP01 모델별 영상 결과 안내

이 폴더는 어떤 영상 모델이 만든 결과인지 구분해서 저장하는 곳입니다.

```text
kling/
seedance/
grok/
```

같은 `CUT_001`을 여러 모델로 시험할 수 있으므로 모델별 폴더를 분리합니다.

파일 이름은 아래 규칙을 권장합니다.

```text
cut_001_v01.mp4
cut_001_v02.mp4
```

가장 좋은 결과를 선택한 뒤에도 원본 후보를 지우지 않고, B단계에서 검수 상태와 선택 결과를 기록하도록 확장합니다.
